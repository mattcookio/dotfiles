# Global Agent Instructions

## Commit Messages
All commits MUST follow conventional commit format with an emoji:

```
<type>[(scope)]: <emoji> <description>
```

- **Types**: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`
- **Emoji**: Required after the colon — any emoji is fine
- **Scope**: Optional, in parentheses
- Examples: `feat(auth): ✨ add SSO login`, `fix: 🐛 resolve null pointer`, `chore(deps): 📦 upgrade typescript`

## PR/Issue Descriptions
- Never use checklists (`- [ ]` or `- [x]`) — use plain bullet points instead
- Never include AI tool attribution or self-citations

## No AI Tool Attribution
Never include any of the following in commits, PRs, or issues:
- `Co-Authored-By:` lines referencing Claude, Codex, opencode, or aider
- `Generated with [Claude Code]`, `Generated with [opencode]`, `Generated by Codex`, etc.
- AI tool noreply emails: `noreply@anthropic.com`, `noreply@openai.com`, `noreply@opencode.ai`, `*@aider.chat`
- AI model name references (e.g. "Claude Sonnet 4.5", "Claude Opus 4.6")
